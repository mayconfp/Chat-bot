services:
  - type: web
    name: chat-bot-bluey
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: streamlit run Projeto/conversation.py --server.port $PORT --server.address 0.0.0.0
    envVars:
      - key: OPENAI_API_KEY
        sync: false 